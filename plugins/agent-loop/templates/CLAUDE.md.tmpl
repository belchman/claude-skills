<!--
  CLAUDE.md stub written by /agent-loop init — ONLY when the repo has no
  CLAUDE.md. Keep under 200 lines (official guidance); prune regularly.
  Placeholders: {{PROJECT_NAME}} {{LANGUAGE}} {{TEST_CMD}} {{LINT_CMD}} {{FORMAT_CMD}}
  Unrecognized values are written as "(fill me in)".
-->
# {{PROJECT_NAME}}

## Shape
- Language/stack: {{LANGUAGE}}
- Tests: `{{TEST_CMD}}`
- Lint: `{{LINT_CMD}}`
- Format: `{{FORMAT_CMD}}`

## Conventions
- (fill me in: naming, layout, patterns worth stating once)

## Restrictions
- (fill me in: what an agent must never touch)

## agent-loop
This repo uses the agent-loop harness. Goal contract: `.claude/agent-loop/GOAL.md`
(+ `goal.json`). Session memory index: @.claude/agent-loop/MEMORY.md
Project canon (stable decisions/architecture): `.claude/agent-loop/vault/`.
Harness health: `/agent-loop doctor`. Loop health: `/agent-loop status`.
